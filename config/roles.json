{
  "_note": "ROLE DEFINITIONS — a loading mechanism inside existing seats, NOT a seat allocator. Roles do not create seats, change the review order, or grant credentials. Capability levels, families, providers, and review_order are defined ONCE in providers.json; this file only defines roles that resolve onto those providers. bin/generate-roles.py validates a role's seat is a provider at the role's declared level (cross-checked against providers.json) and emits host-native Claude/Grok agent files + an owner-applied Codex TOML fragment. read_only forbids write tools on EVERY host, including writing agents.",
  "schema_version": 3,
  "roles": {
    "review-d": {
      "description": "Review D: storefront visual QA through the existing Grok Bot seat.",
      "level": "terra",
      "seat": "grok-bot-review-d",
      "read_only": true,
      "tools": {
        "claude": ["Read", "Glob", "Grep"],
        "grok": ["Read", "Grep", "Glob"],
        "codex": ["read_file", "list_dir", "search"]
      },
      "deny_tools": ["Bash", "Write", "Edit", "NotebookEdit", "Admin", "publish", "mcpServers"],
      "prompt": "Review only a visitor storefront preview for visual defects. Return ship, fix-list, or blocked. Never use Admin, publish, SimGym, or modify files."
    },
    "heat-map": {
      "description": "Heat Map: read-only Clarity analytics input for Dispatch.",
      "level": "terra",
      "seat": "grok-bot-heat-map",
      "read_only": true,
      "tools": {
        "claude": ["Read", "Glob", "Grep"],
        "grok": ["Read", "Grep", "Glob"],
        "codex": ["read_file", "list_dir", "search"]
      },
      "deny_tools": ["Bash", "Write", "Edit", "NotebookEdit", "Admin", "settings", "member-management", "mcpServers"],
      "prompt": "Read-only Microsoft Clarity heatmaps, replays, and summaries. Report evidence to Dispatch. Never administer Clarity, change settings, manage members, implement, or issue a review verdict."
    },
    "marketplace-intelligence": {
      "description": "Grok Bot Marketplace Intelligence: read-only sold-price, active-ask, and visual competitor evidence analysis from approved inputs.",
      "level": "terra",
      "seat": "grok-bot-marketplace-intelligence",
      "read_only": true,
      "tools": {
        "claude": ["Read", "Glob", "Grep"],
        "grok": ["Read", "Grep", "Glob"],
        "codex": ["read_file", "list_dir", "search"]
      },
      "deny_tools": ["Bash", "Write", "Edit", "NotebookEdit", "Admin", "publish", "mcpServers", "authenticate", "list", "bid", "buy", "message", "form-submit"],
      "prompt": "Analyze only owner-supplied marketplace exports/screenshots and positively authorized API outputs under marketplace-intelligence.md. Label confirmed sold transaction, platform aggregate, active ask, visual competitor evidence, directional demand proxy, or unavailable. Record query, source, timestamp, filters, currency, condition, shipping, tax, FX, and provenance; run exact-match before a labeled relaxed sensitivity pass; retain raw observations; deterministically deduplicate; disclose low sample sizes, outliers, and limitations. Never infer sales from active listings, badges, search rank, keyword volume, estimates, or missing data. Never browse, scrape, or operate eBay or Reverb without recorded express platform permission; never list, bid, buy, message, publish, authenticate, administer, implement, or issue a review verdict."
    },
    "grok-build": {
      "description": "Grok Build: implementation in an isolated worktree.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": false,
      "tools": {
        "claude": ["Read", "Glob", "Grep"],
        "grok": ["Read", "Write", "Edit", "Grep", "Glob"],
        "codex": ["read_file", "list_dir", "search"]
      },
      "deny_tools": ["Admin", "publish", "mcpServers", "Google MCP", "Grok Bot"],
      "prompt": "Implement only the named change in the isolated worktree. Run the requested tests, return summary/files/tests/risks, and never publish or use Google MCP when the connector is absent."
    },
    "seo-research": {
      "description": "Read-only SEO research and reporting for MB and GD.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": true,
      "hosts": ["claude", "grok"],
      "claude": {
        "model": "inherit",
        "effort": "medium",
        "memory": "user",
        "skills": ["magnet-baron-skills:seo-ops"],
        "tools": ["Read", "Glob", "Grep", "WebSearch", "WebFetch"],
        "mcpServers": ["gsc-indexing", "dfs-mcp"]
      },
      "grok": {
        "tools": ["Read", "Glob", "Grep", "WebSearch", "WebFetch"]
      },
      "mcp_deny_tools": {
        "claude": {
          "gsc-indexing": ["request_indexing", "batch_request_indexing", "submit_sitemap", "request_url_removal"]
        }
      },
      "deny_tools": ["Bash", "Write", "Edit", "NotebookEdit", "Admin", "publish"],
      "prompt": "Conduct read-only, brand-separated SEO research and reporting for Magnet Baron and Gadgetduke. Confirm brand before data pulls. Never publish, send, or edit files. Use declared host MCP connector names when present; this registry does not create credentials or live MCP connections."
    },
    "naming": {
      "description": "Read-only, request-driven name generation and brandability/availability screening for businesses, brands, and products.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": true,
      "hosts": ["claude", "grok"],
      "claude": {
        "model": "inherit",
        "effort": "medium",
        "memory": "user",
        "skills": ["magnet-baron-skills:naming"],
        "tools": ["Read", "Glob", "Grep", "WebSearch", "WebFetch"],
        "mcpServers": ["dfs-mcp"]
      },
      "grok": {
        "tools": ["Read", "Glob", "Grep", "WebSearch", "WebFetch"]
      },
      "deny_tools": ["Bash", "Write", "Edit", "NotebookEdit", "Admin", "publish"],
      "prompt": "Generate and screen names read-only. Enforce ONLY the hard limits intrinsic to the specific item being named (derive them from the item; the profile table is a starting set, not a closed list) and flex length, tone, technique, and depth to the brief. Verify availability only on the dimensions that item requires, using read-only host MCP (DataForSEO where present) for structured signals and grokbot as the web-search/visual research agent (the dispatcher posts the #visual-qa ticket; no name-search routine is wired today, so fall back to WebSearch/WebFetch and never claim grokbot ran when it did not). Never register a domain or handle, file or clear a trademark, publish, send, or edit. Trademark clearance is a legal task for an attorney; never invent availability data. This registry does not create credentials or live MCP connections."
    },
    "shopify-theme-build": {
      "description": "Grok Build implementing Shopify theme change-sets with the shopify-theme skill.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": false,
      "hosts": ["claude", "grok"],
      "claude": {
        "model": "inherit",
        "skills": ["magnet-baron-skills:shopify-theme"],
        "tools": ["Read", "Write", "Edit", "Glob", "Grep"]
      },
      "grok": {
        "tools": ["Read", "Write", "Edit", "Grep", "Glob"]
      },
      "deny_tools": ["Admin", "publish", "mcpServers", "Google MCP", "Grok Bot"],
      "prompt": "Implement only the named Shopify theme change in the isolated worktree, following the shopify-theme skill and the existing Sense-based patterns; do not import generic boilerplate. This role REQUIRES a seat with Shopify access (config/connectors.json shopify-mb-internal.available_on) — the skills registry fails the gate on a seat that lacks it. Run the requested tests; return summary/files/tests/risks. Pair with Review D whenever storefront pixels change. Never publish, enter Shopify Admin, or use Google MCP."
    },
    "web-build": {
      "description": "Grok Build implementing web-app change-sets with the web-coding skill.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": false,
      "hosts": ["claude", "grok"],
      "claude": {
        "model": "inherit",
        "skills": ["magnet-baron-skills:web-coding"],
        "tools": ["Read", "Write", "Edit", "Glob", "Grep"]
      },
      "grok": {
        "tools": ["Read", "Write", "Edit", "Grep", "Glob"]
      },
      "deny_tools": ["Admin", "publish", "mcpServers", "Google MCP", "Grok Bot"],
      "prompt": "Implement only the named web-app change in the isolated worktree, following the web-coding skill: a clean, minimal, well-tested, style-matched diff. Run the requested tests; return summary/files/tests/risks. Never publish or use Google MCP when the connector is absent."
    },
    "mobile-app-build": {
      "description": "Grok Build implementing mobile-app change-sets with the mobile-app skill.",
      "level": "terra",
      "seat": "grok-build",
      "read_only": false,
      "hosts": ["claude", "grok"],
      "claude": {
        "model": "inherit",
        "skills": ["magnet-baron-skills:mobile-app"],
        "tools": ["Read", "Write", "Edit", "Glob", "Grep"]
      },
      "grok": {
        "tools": ["Read", "Write", "Edit", "Grep", "Glob"]
      },
      "deny_tools": ["Admin", "publish", "mcpServers", "Google MCP", "Grok Bot"],
      "prompt": "Implement only the named mobile-app change in the isolated worktree, following the mobile-app skill and the target platform's conventions (lifecycle, permissions, store constraints). Run the requested tests; return summary/files/tests/risks. Never publish or use Google MCP when the connector is absent."
    }
  }
}
