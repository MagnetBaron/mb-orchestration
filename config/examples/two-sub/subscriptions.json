{
  "_note": "EXAMPLE per-user layer — TWO subscriptions: Codex (dispatch/review) + Grok (implement). Validate: `MB_CONFIG_DIR=config/examples/two-sub python3 bin/doctor.py`. Dispatch (Codex) reserves headroom, but codes when Grok is spent (never-strand). Only one review family (OpenAI) → cross-family gates park unless Review E is wired.",
  "schema_version": 1,
  "owner": "you@example.com",
  "subscriptions": {
    "codex-200": {
      "vendor": "OpenAI", "product": "ChatGPT Pro / Codex $200", "monthly_usd": 200,
      "backs_providers": ["codex-luna", "codex-terra", "codex-sol"], "grants": { "fable": false },
      "notes": "dispatch + MCP + Sol review; reserves headroom, codes as last resort"
    },
    "grok-heavy": {
      "vendor": "xAI", "product": "SuperGrok Heavy", "monthly_usd": null,
      "backs_providers": ["grok-build"], "grants": {},
      "notes": "the implementer"
    }
  }
}
